extends layout 

block content 
w3-card-4 
    header.w3-container.w3-teal 
      h1 Record #{a.id}
    .w3-container.w3-margin-8 
      ul.w3-ul.w3-card-4(style='width:50%')
        li
          b Nome:
            | #{a.nome}
        li
          b Número: 
            | #{a.id}
        li
          b Git Link: 
          a(href='${a.gitlink}') #{a.gitlink}
    .w3-container.w3-margin-8
      ul.w3-ul
        each n in ["tpc1", "tpc2", "tpc3", "tpc4", "tpc5", "tpc6", "tpc7", "tpc8", "tpc9"]
          if n in a 
            - var un = n.toUpperCase()
            li= un 
    footer.w3-container.w3-teal
      address Gerado por galuno::RPCW2022 em #{d} 
        a(href="/") Voltar